import {
  __publicField
} from "./chunk-JVWSFFO4.js";

// node_modules/@hyperframes/player/dist/hyperframes-player.js
function ue(r) {
  return r.hasRuntime || r.runtimeInjected ? false : !!(r.hasNestedCompositions || r.hasTimelines && r.attempts >= 5);
}
function k(r) {
  return typeof r == "object" && r !== null;
}
function pe(r) {
  return k(r) && typeof r.getDuration == "function";
}
function ce(r) {
  return k(r) && typeof r.duration == "function" && typeof r.time == "function" && typeof r.seek == "function" && typeof r.play == "function" && typeof r.pause == "function";
}
var Re = "https://cdn.jsdelivr.net/npm/@hyperframes/core/dist/hyperframe.runtime.iife.js";
function he(r) {
  if (r === null) return null;
  let e = Number.parseInt(r, 10);
  return Number.isFinite(e) && e > 0 ? e : null;
}
function De(r) {
  let e = (r == null ? void 0 : r.querySelector("[data-composition-id][data-width][data-height]")) ?? (r == null ? void 0 : r.querySelector("[data-width][data-height]"));
  if (!e) return null;
  let t = he(e.getAttribute("data-width")), i = he(e.getAttribute("data-height"));
  return t !== null && i !== null ? { width: t, height: i } : null;
}
var O = class {
  constructor(e, t) {
    __publicField(this, "_iframe");
    __publicField(this, "_callbacks");
    __publicField(this, "_interval", null);
    __publicField(this, "_runtimeInjected", false);
    this._iframe = e;
    this._callbacks = t;
  }
  get runtimeInjected() {
    return this._runtimeInjected;
  }
  start() {
    this.stop(), this._runtimeInjected = false;
    let e = 0;
    this._interval = setInterval(() => {
      var _a;
      e++;
      try {
        let t = this._iframe.contentWindow;
        if (!t) return;
        let i = !!(t.__hf || t.__player), n = !!(t.__timelines && Object.keys(t.__timelines).length > 0), o = !!((_a = this._iframe.contentDocument) == null ? void 0 : _a.querySelector("[data-composition-src]"));
        if (ue({ hasRuntime: i, hasTimelines: n, hasNestedCompositions: o, runtimeInjected: this._runtimeInjected, attempts: e })) {
          this._injectRuntime();
          return;
        }
        if (this._runtimeInjected && !i) return;
        let s = this._resolvePlaybackDurationAdapter(t);
        if (s && s.getDuration() > 0) {
          this.stop();
          let l = De(this._iframe.contentDocument);
          this._callbacks.onReady({ duration: s.getDuration(), adapter: s, compositionSize: l });
          return;
        }
      } catch {
      }
      e >= 40 && (this.stop(), this._callbacks.onError("Composition timeline not found after 8s"));
    }, 200);
  }
  stop() {
    this._interval !== null && (clearInterval(this._interval), this._interval = null);
  }
  resolveDirectTimelineAdapter() {
    try {
      let e = this._iframe.contentWindow;
      return e ? this._resolveDirectTimelineAdapterFromWindow(e) : null;
    } catch {
      return null;
    }
  }
  resolveDirectTimelineAdapterFromWindow(e) {
    return this._resolveDirectTimelineAdapterFromWindow(e);
  }
  hasRuntimeBridge(e) {
    return Reflect.get(e, "__hf") !== void 0 || k(Reflect.get(e, "__player"));
  }
  _injectRuntime() {
    var _a, _b;
    this._runtimeInjected = true;
    try {
      let e = this._iframe.contentDocument;
      if (!e) return;
      let t = e.createElement("script");
      t.src = Re, (e.head || e.documentElement).appendChild(t), (_b = (_a = this._callbacks).onRuntimeInjected) == null ? void 0 : _b.call(_a);
    } catch {
    }
  }
  _resolveDirectTimelineAdapterFromWindow(e) {
    var _a, _b;
    if (this.hasRuntimeBridge(e)) return null;
    let t = Reflect.get(e, "__timelines");
    if (!k(t)) return null;
    let i = Object.keys(t);
    if (i.length === 0) return null;
    let n = (_b = (_a = this._iframe.contentDocument) == null ? void 0 : _a.querySelector("[data-composition-id]")) == null ? void 0 : _b.getAttribute("data-composition-id"), o = n && n in t ? n : i[i.length - 1], s = t[o];
    return ce(s) ? s : null;
  }
  _resolvePlaybackDurationAdapter(e) {
    let t = Reflect.get(e, "__player");
    if (pe(t)) return { kind: "runtime", getDuration: () => t.getDuration() };
    let i = this._resolveDirectTimelineAdapterFromWindow(e);
    return i ? { kind: "direct-timeline", timeline: i, getDuration: () => i.duration() } : null;
  }
};
var me = `
  :host {
    display: block;
    position: relative;
    overflow: hidden;
    background: #000;
    contain: layout style;
  }

  .hfp-container {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }


  .hfp-iframe {
    position: absolute;
    top: 50%;
    left: 50%;
    border: none;
    pointer-events: none;
  }

  .hfp-poster {
    position: absolute;
    inset: 0;
    object-fit: contain;
    z-index: 1;
    pointer-events: none;
  }

  .hfp-shader-loader {
    position: absolute;
    inset: 0;
    z-index: 20;
    display: grid;
    place-items: center;
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
    background: #030504;
    color: #f4f7fb;
    cursor: default;
    user-select: none;
    -webkit-user-select: none;
    transition: opacity 420ms ease-out, visibility 420ms ease-out;
  }

  .hfp-shader-loader.hfp-visible,
  .hfp-shader-loader.hfp-hiding {
    visibility: visible;
  }

  .hfp-shader-loader.hfp-visible {
    opacity: 1;
    pointer-events: auto;
  }

  .hfp-shader-loader.hfp-hiding {
    opacity: 0;
    pointer-events: none;
  }

  .hfp-shader-loader-panel {
    display: grid;
    grid-template-rows: 86px 40px 26px 12px 44px;
    justify-items: center;
    align-items: center;
    gap: 8px;
    width: min(620px, 82%);
    text-align: center;
    font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  .hfp-shader-loader-mark {
    width: 86px;
    height: 86px;
    display: grid;
    place-items: center;
    overflow: visible;
  }

  .hfp-shader-loader-mark svg {
    display: block;
    overflow: visible;
    filter: drop-shadow(0 0 5px rgba(79, 219, 94, 0.16));
    pointer-events: none;
  }

  .hfp-shader-loader-title {
    width: 100%;
    height: 40px;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    font-size: 26px;
    line-height: 40px;
    font-weight: 700;
    letter-spacing: 0;
  }

  .hfp-shader-loader-title-text {
    color: transparent;
    background: linear-gradient(
      90deg,
      rgba(244, 247, 251, 0.84) 0%,
      #ffffff 42%,
      #80efe4 52%,
      #ffffff 62%,
      rgba(244, 247, 251, 0.84) 100%
    );
    background-size: 220% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    animation: hfp-shader-loader-sheen 1.9s linear infinite;
  }

  .hfp-shader-loader-detail {
    width: 100%;
    height: 26px;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
    color: rgba(244, 247, 251, 0.62);
    font-size: 15px;
    line-height: 26px;
    font-weight: 500;
  }

  .hfp-shader-loader-track {
    width: min(360px, 100%);
    height: 8px;
    overflow: hidden;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.1);
  }

  .hfp-shader-loader-fill {
    width: 100%;
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #06e3fa, #4fdb5e);
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 160ms ease;
  }

  .hfp-shader-loader-progress {
    width: min(420px, 100%);
    height: 44px;
    display: grid;
    grid-template-rows: repeat(2, 22px);
    color: rgba(244, 247, 251, 0.48);
    font: 600 13px/22px "IBM Plex Mono", "SF Mono", "Fira Code", "Courier New", monospace;
    font-variant-numeric: tabular-nums;
  }

  .hfp-shader-loader-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 74px;
    align-items: center;
    column-gap: 20px;
    width: 100%;
    white-space: nowrap;
  }

  .hfp-shader-loader-label {
    min-width: 0;
    overflow: hidden;
    text-align: left;
    text-overflow: ellipsis;
  }

  .hfp-shader-loader-value {
    text-align: right;
  }

  @keyframes hfp-shader-loader-sheen {
    from {
      background-position: 140% 0;
    }
    to {
      background-position: -140% 0;
    }
  }

  /* ── Theming via CSS custom properties ──
   *
   * Override from outside the shadow DOM:
   *   hyperframes-player {
   *     --hfp-controls-bg: linear-gradient(transparent, rgba(0,0,0,0.9));
   *     --hfp-accent: #ff6b6b;
   *     --hfp-font: "Inter", sans-serif;
   *   }
   */

  .hfp-controls {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    align-items: center;
    gap: var(--hfp-controls-gap, 12px);
    padding: var(--hfp-controls-padding, 8px 16px);
    background: var(--hfp-controls-bg, linear-gradient(transparent, rgba(0, 0, 0, 0.7)));
    color: var(--hfp-color, #fff);
    font-family: var(--hfp-font, system-ui, -apple-system, sans-serif);
    font-size: var(--hfp-font-size, 13px);
    z-index: 10;
    pointer-events: auto;
    opacity: 1;
    transition: opacity 0.3s ease;
    user-select: none;
  }

  .hfp-controls.hfp-hidden {
    opacity: 0;
    pointer-events: none;
  }

  .hfp-play-btn {
    background: none;
    border: none;
    color: var(--hfp-color, #fff);
    cursor: pointer;
    padding: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    flex-shrink: 0;
    z-index: 10;
  }

  .hfp-play-btn:hover {
    opacity: 0.8;
  }

  .hfp-play-btn svg,
  .hfp-play-btn svg * {
    pointer-events: none;
  }

  .hfp-scrubber {
    flex: 1;
    min-width: 0;
    height: var(--hfp-scrubber-height, 4px);
    background: var(--hfp-scrubber-bg, rgba(255, 255, 255, 0.3));
    border-radius: var(--hfp-scrubber-radius, 2px);
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .hfp-scrubber:hover {
    height: var(--hfp-scrubber-height-hover, 6px);
  }

  .hfp-progress {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    background: var(--hfp-accent, #fff);
    pointer-events: none;
  }

  .hfp-time {
    flex-shrink: 0;
    font-variant-numeric: tabular-nums;
    opacity: 0.9;
  }

  .hfp-speed-wrap {
    position: relative;
    flex-shrink: 0;
  }

  .hfp-speed-btn {
    background: var(--hfp-speed-btn-bg, rgba(255, 255, 255, 0.15));
    border: none;
    border-radius: var(--hfp-speed-btn-radius, 4px);
    color: var(--hfp-color, #fff);
    cursor: pointer;
    font-family: var(--hfp-font, system-ui, -apple-system, sans-serif);
    font-size: 12px;
    font-variant-numeric: tabular-nums;
    font-weight: 600;
    padding: 4px 8px;
    min-width: 40px;
    text-align: center;
    transition: background 0.15s ease;
  }

  .hfp-speed-btn:hover {
    background: var(--hfp-speed-btn-bg-hover, rgba(255, 255, 255, 0.3));
  }

  .hfp-speed-menu {
    position: absolute;
    bottom: calc(100% + 8px);
    right: 0;
    background: var(--hfp-menu-bg, rgba(20, 20, 20, 0.95));
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid var(--hfp-menu-border, rgba(255, 255, 255, 0.1));
    border-radius: var(--hfp-menu-radius, 8px);
    padding: 4px;
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 80px;
    opacity: 0;
    visibility: hidden;
    transform: translateY(4px);
    transition: opacity 0.15s ease, transform 0.15s ease, visibility 0.15s;
    box-shadow: var(--hfp-menu-shadow, 0 8px 24px rgba(0, 0, 0, 0.4));
  }

  .hfp-speed-menu.hfp-open {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .hfp-speed-option {
    background: none;
    border: none;
    border-radius: 4px;
    color: var(--hfp-menu-color, rgba(255, 255, 255, 0.7));
    cursor: pointer;
    font-family: var(--hfp-font, system-ui, -apple-system, sans-serif);
    font-size: 13px;
    font-variant-numeric: tabular-nums;
    padding: 6px 12px;
    text-align: left;
    transition: background 0.1s ease, color 0.1s ease;
    white-space: nowrap;
  }

  .hfp-speed-option:hover {
    background: var(--hfp-menu-hover-bg, rgba(255, 255, 255, 0.1));
    color: var(--hfp-color, #fff);
  }

  .hfp-speed-option.hfp-active {
    color: var(--hfp-accent, #fff);
    font-weight: 600;
  }

  .hfp-volume-wrap {
    position: relative;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    gap: 0;
  }

  .hfp-mute-btn {
    background: none;
    border: none;
    color: var(--hfp-color, #fff);
    cursor: pointer;
    padding: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    flex-shrink: 0;
  }

  .hfp-mute-btn:hover {
    opacity: 0.8;
  }

  .hfp-mute-btn svg,
  .hfp-mute-btn svg * {
    pointer-events: none;
  }

  .hfp-volume-slider-wrap {
    width: 0;
    overflow: hidden;
    transition: width 0.2s ease;
    display: flex;
    align-items: center;
  }

  .hfp-volume-wrap:hover .hfp-volume-slider-wrap {
    width: 64px;
  }

  .hfp-volume-slider {
    width: 56px;
    height: var(--hfp-scrubber-height, 4px);
    background: var(--hfp-scrubber-bg, rgba(255, 255, 255, 0.3));
    border-radius: var(--hfp-scrubber-radius, 2px);
    cursor: pointer;
    position: relative;
    overflow: hidden;
    margin-left: 4px;
    margin-right: 4px;
  }

  .hfp-volume-fill {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    background: var(--hfp-accent, #fff);
    pointer-events: none;
  }
`;
var $ = '<svg width="24" height="24" viewBox="0 0 18 18" fill="currentColor"><polygon points="4,2 16,9 4,16"/></svg>';
var fe = '<svg width="24" height="24" viewBox="0 0 18 18" fill="currentColor"><rect x="3" y="2" width="4" height="14"/><rect x="11" y="2" width="4" height="14"/></svg>';
var W = '<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M3 9v6h4l5 5V4L7 9H3z"/><path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02z"/><path d="M14 3.23v2.06c2.89.86 5 3.54 5 6.71s-2.11 5.85-5 6.71v2.06c4.01-.91 7-4.49 7-8.77s-2.99-7.86-7-8.77z"/></svg>';
var q = '<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M3 9v6h4l5 5V4L7 9H3z"/><path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02z"/></svg>';
var be = '<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M3 9v6h4l5 5V4L7 9H3z"/><path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02z" opacity="0.3"/><line x1="18" y1="7" x2="14" y2="17" stroke="currentColor" stroke-width="2"/></svg>';
var ve = [0.25, 0.5, 1, 1.5, 2, 4];
function H(r) {
  return Number.isInteger(r) ? `${r}x` : `${r}x`;
}
function B(r) {
  if (!Number.isFinite(r) || r < 0) return "0:00";
  let e = Math.floor(r), t = Math.floor(e / 60), i = e % 60;
  return `${t}:${i.toString().padStart(2, "0")}`;
}
function ge(r, e, t = {}) {
  let i = t.speedPresets ?? ve, n = document.createElement("div");
  n.className = "hfp-controls", n.addEventListener("click", (a) => {
    a.stopPropagation();
  });
  let o = document.createElement("button");
  o.className = "hfp-play-btn", o.type = "button", o.innerHTML = $, o.setAttribute("aria-label", "Play");
  let s = document.createElement("div");
  s.className = "hfp-scrubber";
  let l = document.createElement("div");
  l.className = "hfp-progress", l.style.width = "0%", s.appendChild(l);
  let u = document.createElement("span");
  u.className = "hfp-time", u.textContent = "0:00 / 0:00";
  let c = document.createElement("div");
  c.className = "hfp-speed-wrap";
  let p = document.createElement("button");
  p.className = "hfp-speed-btn", p.type = "button", p.textContent = "1x", p.setAttribute("aria-label", "Playback speed");
  let b = document.createElement("div");
  b.className = "hfp-speed-menu", b.setAttribute("role", "menu");
  for (let a of i) {
    let d = document.createElement("button");
    d.className = "hfp-speed-option", d.type = "button", d.setAttribute("role", "menuitem"), d.dataset.speed = String(a), d.textContent = H(a), a === 1 && d.classList.add("hfp-active"), b.appendChild(d);
  }
  c.appendChild(b), c.appendChild(p);
  let _ = document.createElement("div");
  _.className = "hfp-volume-wrap";
  let m = document.createElement("button");
  m.className = "hfp-mute-btn", m.type = "button", m.innerHTML = W, m.setAttribute("aria-label", "Mute");
  let g = document.createElement("div");
  g.className = "hfp-volume-slider-wrap";
  let h = document.createElement("div");
  h.className = "hfp-volume-slider", h.setAttribute("role", "slider"), h.setAttribute("aria-label", "Volume"), h.setAttribute("aria-valuemin", "0"), h.setAttribute("aria-valuemax", "100"), h.setAttribute("aria-valuenow", "100"), h.tabIndex = 0;
  let v = document.createElement("div");
  v.className = "hfp-volume-fill", v.style.width = "100%", h.appendChild(v), g.appendChild(h), _.appendChild(g), _.appendChild(m), n.appendChild(o), n.appendChild(s), n.appendChild(u), n.appendChild(_), n.appendChild(c), r.appendChild(n);
  let L = false, S = false, y = 1, M = null, P = i.indexOf(1);
  P === -1 && (P = 0);
  let R = (a, d) => a ? be : d === 0 ? q : d < 0.5 ? q : W;
  o.addEventListener("click", (a) => {
    a.stopPropagation(), L ? e.onPause() : e.onPlay();
  }), m.addEventListener("click", (a) => {
    a.stopPropagation(), e.onMuteToggle();
  });
  let T = false, D = (a) => {
    let d = h.getBoundingClientRect(), f = Math.max(0, Math.min(1, (a - d.left) / d.width));
    y = f, v.style.width = `${f * 100}%`, h.setAttribute("aria-valuenow", String(Math.round(f * 100))), S && f > 0 && e.onMuteToggle(), m.innerHTML = R(S, f), e.onVolumeChange(f);
  };
  h.addEventListener("mousedown", (a) => {
    a.stopPropagation(), T = true, D(a.clientX);
  });
  let J = (a) => {
    T && D(a.clientX);
  }, K = () => {
    T = false;
  };
  document.addEventListener("mousemove", J), document.addEventListener("mouseup", K), h.addEventListener("touchstart", (a) => {
    T = true;
    let d = a.touches[0];
    d && D(d.clientX);
  }, { passive: true });
  let Z = (a) => {
    if (T) {
      let d = a.touches[0];
      d && D(d.clientX);
    }
  }, ee = () => {
    T = false;
  };
  document.addEventListener("touchmove", Z, { passive: true }), document.addEventListener("touchend", ee);
  let te = 0.05;
  h.addEventListener("keydown", (a) => {
    let d = y;
    if (a.key === "ArrowRight" || a.key === "ArrowUp") d = Math.min(1, y + te);
    else if (a.key === "ArrowLeft" || a.key === "ArrowDown") d = Math.max(0, y - te);
    else return;
    a.preventDefault(), a.stopPropagation(), y = d, v.style.width = `${d * 100}%`, h.setAttribute("aria-valuenow", String(Math.round(d * 100))), S && d > 0 && e.onMuteToggle(), m.innerHTML = R(S, d), e.onVolumeChange(d);
  });
  let ie = (a) => {
    for (let d of b.querySelectorAll(".hfp-speed-option")) d.classList.toggle("hfp-active", d.dataset.speed === String(a));
  };
  p.addEventListener("click", (a) => {
    a.stopPropagation();
    let d = b.classList.toggle("hfp-open");
    p.setAttribute("aria-expanded", String(d));
  }), b.addEventListener("click", (a) => {
    a.stopPropagation();
    let d = a.target.closest(".hfp-speed-option");
    if (!d) return;
    let f = parseFloat(d.dataset.speed);
    P = i.indexOf(f), p.textContent = H(f), ie(f), b.classList.remove("hfp-open"), p.setAttribute("aria-expanded", "false"), e.onSpeedChange(f);
  });
  let re = () => {
    b.classList.remove("hfp-open"), p.setAttribute("aria-expanded", "false");
  };
  document.addEventListener("click", re);
  let I = (a) => {
    let d = s.getBoundingClientRect(), f = Math.max(0, Math.min(1, (a - d.left) / d.width));
    e.onSeek(f);
  }, w = false;
  s.addEventListener("mousedown", (a) => {
    a.stopPropagation(), w = true, I(a.clientX);
  });
  let ne = (a) => {
    w && I(a.clientX);
  }, oe = () => {
    w = false;
  };
  document.addEventListener("mousemove", ne), document.addEventListener("mouseup", oe), s.addEventListener("touchstart", (a) => {
    w = true;
    let d = a.touches[0];
    d && I(d.clientX);
  }, { passive: true });
  let ae = (a) => {
    if (w) {
      let d = a.touches[0];
      d && I(d.clientX);
    }
  }, se = () => {
    w = false;
  };
  document.addEventListener("touchmove", ae, { passive: true }), document.addEventListener("touchend", se);
  let le = () => {
    M && clearTimeout(M), M = setTimeout(() => {
      L && n.classList.add("hfp-hidden");
    }, 3e3);
  }, de = r instanceof ShadowRoot ? r.host : r;
  return de.addEventListener("mousemove", () => {
    n.classList.remove("hfp-hidden"), le();
  }), de.addEventListener("mouseleave", () => {
    L && n.classList.add("hfp-hidden");
  }), { updateTime(a, d) {
    let f = d > 0 ? Math.min(a, d) : a, Pe = d > 0 ? f / d * 100 : 0;
    l.style.width = `${Pe}%`, u.textContent = `${B(f)} / ${B(d)}`;
  }, updatePlaying(a) {
    L = a, o.innerHTML = a ? fe : $, o.setAttribute("aria-label", a ? "Pause" : "Play"), a ? le() : n.classList.remove("hfp-hidden");
  }, updateSpeed(a) {
    let d = i.indexOf(a);
    d !== -1 && (P = d), p.textContent = H(a), ie(a);
  }, updateMuted(a) {
    S = a, m.innerHTML = R(a, y), m.setAttribute("aria-label", a ? "Unmute" : "Mute");
  }, updateVolume(a) {
    y = a, v.style.width = `${a * 100}%`, h.setAttribute("aria-valuenow", String(Math.round(a * 100))), m.innerHTML = R(S, a);
  }, show() {
    n.style.display = "";
  }, hide() {
    n.style.display = "none";
  }, destroy() {
    document.removeEventListener("mousemove", ne), document.removeEventListener("mouseup", oe), document.removeEventListener("touchmove", ae), document.removeEventListener("touchend", se), document.removeEventListener("mousemove", J), document.removeEventListener("mouseup", K), document.removeEventListener("touchmove", Z), document.removeEventListener("touchend", ee), document.removeEventListener("click", re), M && clearTimeout(M);
  } };
}
function _e(r, e, t, i, n) {
  let o = i ? i.split(",").map(Number).filter((u) => !isNaN(u) && u > 0) : void 0, l = ge(r, n, o ? { speedPresets: o } : {});
  return l.updateMuted(e), l.updateVolume(t), l;
}
function G(r, e, t) {
  return e ? (t || (t = document.createElement("img"), t.className = "hfp-poster", r.appendChild(t)), t.src = e, t) : (t == null ? void 0 : t.remove(), null);
}
function ye(r) {
  return r.composedPath().some((e) => e instanceof HTMLElement && e.classList.contains("hfp-controls"));
}
var N = null;
function Ee(r, e) {
  if (typeof CSSStyleSheet < "u") try {
    N || (N = new CSSStyleSheet(), N.replaceSync(e)), r.adoptedStyleSheets = [N];
    return;
  } catch {
  }
  let t = document.createElement("style");
  t.textContent = e, r.appendChild(t);
}
function Se() {
  let r = document.createElement("div");
  r.className = "hfp-container";
  let e = document.createElement("iframe");
  return e.className = "hfp-iframe", e.sandbox.add("allow-scripts", "allow-same-origin"), e.allow = "autoplay; fullscreen", e.referrerPolicy = "no-referrer", e.title = "HyperFrames Composition", r.appendChild(e), { container: r, iframe: e };
}
function Te(r, e, t, i) {
  let n = r.offsetWidth, o = r.offsetHeight;
  if (n === 0 || o === 0) return;
  let s = Math.min(n / t, o / i);
  e.style.width = `${t}px`, e.style.height = `${i}px`, e.style.transform = `translate(-50%, -50%) scale(${s})`;
}
var F = class {
  constructor(e) {
    __publicField(this, "_callbacks");
    __publicField(this, "_raf", null);
    __publicField(this, "_lastUpdateMs", 0);
    this._callbacks = e;
  }
  start(e, t, i, n) {
    this.stop();
    let o = () => {
      if (n()) {
        this._raf = null;
        return;
      }
      let s;
      try {
        s = e.time();
      } catch {
        this._raf = null;
        return;
      }
      let l = i();
      l > 0 && (s = Math.min(s, l));
      let u = l > 0 && s >= l, c = performance.now();
      if ((c - this._lastUpdateMs > 100 || u) && (this._lastUpdateMs = c, this._callbacks.onTimeUpdate(s, l)), u) {
        if (this._callbacks.getLoop()) {
          this._callbacks.restart();
          return;
        }
        try {
          e.pause();
        } catch {
        }
        this._callbacks.onPaused(), this._raf = null;
        return;
      }
      this._raf = requestAnimationFrame(o);
    };
    this._raf = requestAnimationFrame(o);
  }
  stop() {
    this._raf !== null && (cancelAnimationFrame(this._raf), this._raf = null);
  }
  get isRunning() {
    return this._raf !== null;
  }
};
function we(r) {
  let e = Array.from(r.querySelectorAll("[data-composition-id]"));
  if (e.length === 0) return r.body ? [r.body] : [];
  let t = [];
  for (let i of e) Oe(i) || t.push(i);
  return Ie(r), t;
}
function Ie(r) {
  let e = r.body;
  if (!e || typeof console > "u" || typeof console.warn != "function") return;
  let t = e.querySelectorAll("audio[data-start], video[data-start]");
  if (t.length === 0) return;
  let i = [];
  for (let n of t) n.closest("[data-composition-id]") || i.push(n);
  i.length !== 0 && console.warn(`[hyperframes-player] selectMediaObserverTargets: composition hosts are present, but ${i.length} body-level timed media element(s) sit outside every [data-composition-id] subtree and will not be observed. Move them inside a composition host or the parent-frame proxy will never adopt them.`, i);
}
function Oe(r) {
  let e = r.parentElement;
  for (; e; ) {
    if (e.hasAttribute("data-composition-id")) return true;
    e = e.parentElement;
  }
  return false;
}
var He = 0.05;
var Ne = 2;
var U = class {
  constructor(e) {
    __publicField(this, "_entries", []);
    __publicField(this, "_mediaObserver");
    __publicField(this, "_playbackErrorPosted", false);
    __publicField(this, "_audioOwner", "runtime");
    __publicField(this, "_dispatchEvent");
    __publicField(this, "_getMuted");
    __publicField(this, "_getVolume");
    __publicField(this, "_getPlaybackRate");
    __publicField(this, "_getCurrentTime");
    __publicField(this, "_isPaused");
    this._dispatchEvent = e.dispatchEvent, this._getMuted = e.getMuted, this._getVolume = e.getVolume, this._getPlaybackRate = e.getPlaybackRate, this._getCurrentTime = e.getCurrentTime, this._isPaused = e.isPaused;
  }
  get audioOwner() {
    return this._audioOwner;
  }
  get entries() {
    return this._entries;
  }
  get playbackErrorPosted() {
    return this._playbackErrorPosted;
  }
  resetForIframeLoad() {
    this._playbackErrorPosted = false;
    let e = this._audioOwner === "parent";
    this._audioOwner = "runtime", this.pauseAll(), this.teardownObserver(), e && this._dispatchEvent(new CustomEvent("audioownershipchange", { detail: { owner: "runtime", reason: "iframe-reload" } }));
  }
  destroy() {
    this.teardownObserver();
    for (let e of this._entries) e.el.pause(), e.el.src = "";
    this._entries = [];
  }
  updateMuted(e) {
    for (let t of this._entries) t.el.muted = e;
  }
  updateVolume(e) {
    for (let t of this._entries) t.el.volume = e;
  }
  updatePlaybackRate(e) {
    for (let t of this._entries) t.el.playbackRate = e;
  }
  playAll() {
    for (let e of this._entries) e.el.src && e.el.play().catch((t) => this._reportPlaybackError(t));
  }
  pauseAll() {
    for (let e of this._entries) e.el.pause();
  }
  seekAll(e) {
    for (let t of this._entries) {
      let i = e - t.start;
      i >= 0 && i < t.duration && (t.el.currentTime = i);
    }
  }
  mirrorTime(e, t) {
    let i = (t == null ? void 0 : t.force) === true;
    for (let n of this._entries) {
      let o = e - n.start;
      if (o < 0 || o >= n.duration) {
        n.driftSamples = 0;
        continue;
      }
      Math.abs(n.el.currentTime - o) > He ? (n.driftSamples += 1, (i || n.driftSamples >= Ne) && (n.el.currentTime = o, n.driftSamples = 0)) : n.driftSamples = 0;
    }
  }
  promoteToParentProxy(e, t) {
    if (this._audioOwner === "parent") return;
    if (this._audioOwner = "parent", e) for (let n of e.querySelectorAll("video, audio")) n.muted = true;
    let i = this._getCurrentTime();
    t ? t(i, { force: true }) : this.mirrorTime(i, { force: true }), this._isPaused() || this.playAll(), this._dispatchEvent(new CustomEvent("audioownershipchange", { detail: { owner: "parent", reason: "autoplay-blocked" } }));
  }
  setupFromIframe(e) {
    let t = e.querySelectorAll("audio[data-start], video[data-start]");
    for (let i of t) this._adoptIframeMedia(i);
    this._observeDynamicMedia(e);
  }
  setupFromUrl(e) {
    this._createEntry(e, "audio", 0, 1 / 0);
  }
  teardownObserver() {
    var _a;
    (_a = this._mediaObserver) == null ? void 0 : _a.disconnect(), this._mediaObserver = void 0;
  }
  _reportPlaybackError(e) {
    this._playbackErrorPosted || (this._playbackErrorPosted = true, this._dispatchEvent(new CustomEvent("playbackerror", { detail: { source: "parent-proxy", error: e } })));
  }
  _createEntry(e, t, i, n) {
    if (this._entries.some((u) => u.el.src === e)) return null;
    let o = t === "video" ? document.createElement("video") : new Audio();
    o.preload = "auto", o.src = e, o.load(), o.muted = this._getMuted(), o.volume = this._getVolume();
    let s = this._getPlaybackRate();
    s !== 1 && (o.playbackRate = s);
    let l = { el: o, start: i, duration: n, driftSamples: 0 };
    return this._entries.push(l), l;
  }
  _adoptIframeMedia(e) {
    var _a;
    if (e.preload === "metadata" || e.preload === "none") return;
    let t = e.getAttribute("src") || ((_a = e.querySelector("source")) == null ? void 0 : _a.getAttribute("src"));
    if (!t) return;
    let i = new URL(t, e.ownerDocument.baseURI).href, n = parseFloat(e.getAttribute("data-start") || "0"), o = parseFloat(e.getAttribute("data-duration") || "Infinity"), s = e.tagName === "VIDEO" ? "video" : "audio", l = this._createEntry(i, s, n, o);
    l && this._audioOwner === "parent" && (this.mirrorTime(this._getCurrentTime(), { force: true }), !this._isPaused() && l.el.src && l.el.play().catch((u) => this._reportPlaybackError(u)));
  }
  _detachIframeMedia(e) {
    var _a;
    let t = e.getAttribute("src") || ((_a = e.querySelector("source")) == null ? void 0 : _a.getAttribute("src"));
    if (!t) return;
    let i = new URL(t, e.ownerDocument.baseURI).href, n = this._entries.findIndex((s) => s.el.src === i);
    if (n === -1) return;
    let o = this._entries[n];
    o.el.pause(), o.el.src = "", this._entries.splice(n, 1);
  }
  _observeDynamicMedia(e) {
    if (this.teardownObserver(), typeof MutationObserver > "u" || !e.body) return;
    let t = new MutationObserver((o) => {
      var _a, _b, _c, _d;
      for (let s of o) {
        if (s.type === "attributes" && s.attributeName === "preload") {
          let l = s.target;
          l instanceof HTMLMediaElement && l.matches("audio[data-start], video[data-start]") && l.preload === "auto" && this._adoptIframeMedia(l);
          continue;
        }
        for (let l of s.addedNodes) {
          if (!(l instanceof Element)) continue;
          let u = [];
          ((_a = l.matches) == null ? void 0 : _a.call(l, "audio[data-start], video[data-start]")) && u.push(l);
          let c = (_b = l.querySelectorAll) == null ? void 0 : _b.call(l, "audio[data-start], video[data-start]");
          if (c) for (let p of c) u.push(p);
          for (let p of u) this._adoptIframeMedia(p);
        }
        for (let l of s.removedNodes) {
          if (!(l instanceof Element)) continue;
          let u = [];
          ((_c = l.matches) == null ? void 0 : _c.call(l, "audio[data-start], video[data-start]")) && u.push(l);
          let c = (_d = l.querySelectorAll) == null ? void 0 : _d.call(l, "audio[data-start], video[data-start]");
          if (c) for (let p of c) u.push(p);
          for (let p of u) this._detachIframeMedia(p);
        }
      }
    }), i = { childList: true, subtree: true, attributes: true, attributeFilter: ["preload"] }, n = we(e);
    for (let o of n) t.observe(o, i);
    this._mediaObserver = t;
  }
};
function xe(r, e, t, i) {
  let n = (r.frame ?? 0) / e, o = t.duration > 0 ? Math.min(n, t.duration) : n, s = !t.paused, l = !r.isPlaying, u = t.duration > 0 && o >= t.duration && (s || r.isPlaying);
  if (u && i.getLoop()) return i.media.audioOwner === "parent" && i.media.pauseAll(), i.seek(0), i.play(), { ...t, currentTime: o, paused: false };
  let c = { ...t, currentTime: o, paused: l };
  i.media.audioOwner === "parent" && (s && l ? i.media.pauseAll() : !s && !l && i.media.playAll(), i.media.mirrorTime(o));
  let p = performance.now(), b = l !== t.paused;
  return (p - t.lastUpdateMs > 100 || b) && (c.lastUpdateMs = p, i.updateControlsTime(o, t.duration), i.updateControlsPlaying(!l), i.dispatchEvent(new CustomEvent("timeupdate", { detail: { currentTime: o } }))), u && (i.media.audioOwner === "parent" && i.media.pauseAll(), c.paused = true, i.updateControlsPlaying(false), i.dispatchEvent(new Event("ended"))), c;
}
var Ce = 30;
function Ae(r, e, t) {
  if (r.source !== e) return;
  let i = r.data;
  if (!(!i || i.source !== "hf-preview")) {
    if (i.type === "shader-transition-state") {
      let n = i.state && typeof i.state == "object" ? i.state : {};
      t.shaderLoader.update(n, t.getShaderLoadingMode()), t.dispatchEvent(new CustomEvent("shadertransitionstate", { detail: { compositionId: i.compositionId, state: n } }));
      return;
    }
    if (i.type === "state") {
      t.setPlaybackState(xe({ frame: i.frame ?? 0, isPlaying: !!i.isPlaying }, Ce, t.getPlaybackState(), t));
      return;
    }
    if (i.type === "media-autoplay-blocked") {
      let n = null;
      try {
        n = t.getIframeDoc();
      } catch {
      }
      t.media.promoteToParentProxy(n, (o, s) => t.media.mirrorTime(o, s)), t.sendControl("set-media-output-muted", { muted: true });
      return;
    }
    if (i.type === "timeline" && i.durationInFrames > 0) {
      if (Number.isFinite(i.durationInFrames)) {
        let n = t.getPlaybackState(), o = i.durationInFrames / Ce;
        t.setPlaybackState({ ...n, duration: o }), t.updateControlsTime(n.currentTime, o);
      }
      return;
    }
    i.type === "stage-size" && i.width > 0 && i.height > 0 && t.setCompositionSize(i.width, i.height);
  }
}
var E = "shader-capture-scale";
var x = "shader-loading";
var Fe = "__hf_shader_capture_scale";
var Ue = "__hf_shader_loading";
var C = ["Preparing scene transitions", "Sampling outgoing scene motion", "Sampling incoming scene motion", "Caching transition frames", "Finalizing transition preview"];
function X(r) {
  if (r === null) return null;
  let e = Number(r);
  return !Number.isFinite(e) || e <= 0 ? null : String(Math.min(1, Math.max(0.25, e)));
}
function Ve(r) {
  if (r === null || r.trim() === "") return "composition";
  let e = r.trim().toLowerCase();
  return e === "none" || e === "false" || e === "0" || e === "off" ? "none" : e === "player" || e === "true" || e === "1" || e === "on" ? "player" : "composition";
}
function Me(r, e, t) {
  t === null ? r.delete(e) : r.set(e, t);
}
function je(r, e, t) {
  let i = r.indexOf("#"), n = i >= 0 ? r.slice(0, i) : r, o = i >= 0 ? r.slice(i) : "", s = n.indexOf("?"), l = s >= 0 ? n.slice(0, s) : n, u = s >= 0 ? n.slice(s + 1) : "", c = new URLSearchParams(u);
  Me(c, Fe, e), Me(c, Ue, t === "composition" ? null : t);
  let p = c.toString();
  return `${l}${p ? `?${p}` : ""}${o}`;
}
function ze(r, e, t) {
  if (e === null && t === "composition") return r;
  let i = [];
  e !== null && i.push(`window.__HF_SHADER_CAPTURE_SCALE=${JSON.stringify(e)};`), t !== "composition" && i.push(`window.__HF_SHADER_LOADING=${JSON.stringify(t)};`);
  let n = `<script data-hyperframes-player-shader-options>${i.join("")}<\/script>`;
  return /<head\b[^>]*>/i.test(r) ? r.replace(/<head\b[^>]*>/i, (o) => `${o}${n}`) : /<html\b[^>]*>/i.test(r) ? r.replace(/<html\b[^>]*>/i, (o) => `${o}${n}`) : `${n}${r}`;
}
function A(r) {
  return Ve(r.getAttribute(x));
}
function ke(r) {
  return Number(X(r.getAttribute(E)) ?? "1");
}
function V(r, e) {
  return je(e, X(r.getAttribute(E)), A(r));
}
function j(r, e) {
  return ze(e, X(r.getAttribute(E)), A(r));
}
function Le() {
  let r = document.createElement("div");
  r.className = "hfp-shader-loader", r.setAttribute("role", "status"), r.setAttribute("aria-live", "polite"), r.setAttribute("aria-label", "Preparing scene transitions"), r.setAttribute("data-hyperframes-ignore", ""), r.draggable = false;
  let e = (m) => {
    m.preventDefault(), m.stopPropagation();
  };
  for (let m of ["selectstart", "dragstart", "pointerdown", "mousedown", "click", "dblclick", "contextmenu", "touchstart"]) r.addEventListener(m, e, { capture: true });
  let t = document.createElement("div");
  t.className = "hfp-shader-loader-panel", t.draggable = false;
  let i = document.createElement("div");
  i.className = "hfp-shader-loader-mark", i.draggable = false, i.innerHTML = ['<svg width="78" height="78" viewBox="0 0 100 100" fill="none" aria-hidden="true" draggable="false">', '<path d="M10.1851 57.8021L33.1145 73.8313C36.2202 75.9978 41.5173 73.5433 42.4816 69.4984L51.7611 30.4271C52.7253 26.3822 48.5802 23.9277 44.4602 26.0942L13.917 42.1235C6.96677 45.7676 4.97564 54.1579 10.1851 57.8021Z" fill="url(#hfp-shader-loader-grad-left)"/>', '<path d="M87.5129 57.5141L56.9696 73.5433C52.8371 75.7098 48.7046 73.2553 49.6688 69.2104L58.9483 30.1391C59.9125 26.0942 65.2097 23.6397 68.3154 25.8062L91.2447 41.8354C96.4668 45.4796 94.4631 53.8699 87.5129 57.5141Z" fill="url(#hfp-shader-loader-grad-right)"/>', "<defs>", '<linearGradient id="hfp-shader-loader-grad-left" x1="48.5676" y1="25" x2="44.7804" y2="71.9384" gradientUnits="userSpaceOnUse">', '<stop stop-color="#06E3FA"/>', '<stop offset="1" stop-color="#4FDB5E"/>', "</linearGradient>", '<linearGradient id="hfp-shader-loader-grad-right" x1="54.8282" y1="73.8392" x2="72.0989" y2="32.8932" gradientUnits="userSpaceOnUse">', '<stop stop-color="#06E3FA"/>', '<stop offset="1" stop-color="#4FDB5E"/>', "</linearGradient>", "</defs>", "</svg>"].join("");
  let n = document.createElement("div");
  n.className = "hfp-shader-loader-title";
  let o = document.createElement("span");
  o.className = "hfp-shader-loader-title-text", o.textContent = C[0] || "Preparing scene transitions", n.appendChild(o);
  let s = document.createElement("div");
  s.className = "hfp-shader-loader-detail", s.textContent = "Rendering animated scene samples for shader transitions.";
  let l = document.createElement("div");
  l.className = "hfp-shader-loader-track", l.setAttribute("aria-hidden", "true");
  let u = document.createElement("div");
  u.className = "hfp-shader-loader-fill", l.appendChild(u);
  let c = document.createElement("div");
  c.className = "hfp-shader-loader-progress";
  let p = (m) => {
    let g = document.createElement("div");
    g.className = "hfp-shader-loader-row";
    let h = document.createElement("span");
    h.className = "hfp-shader-loader-label", h.textContent = m;
    let v = document.createElement("span");
    return v.className = "hfp-shader-loader-value", g.appendChild(h), g.appendChild(v), c.appendChild(g), { row: g, label: h, value: v };
  }, b = p("transition"), _ = p("transition frame");
  return t.appendChild(i), t.appendChild(n), t.appendChild(s), t.appendChild(l), t.appendChild(c), r.appendChild(t), { root: r, fill: u, title: o, detail: s, transitionValue: b.value, frameLabel: _.label, frameValue: _.value, frameRow: _.row };
}
var $e = 420;
var z = class {
  constructor(e) {
    __publicField(this, "_el");
    __publicField(this, "_hideTimeout", null);
    this._el = e;
  }
  show() {
    this._hideTimeout && (clearTimeout(this._hideTimeout), this._hideTimeout = null), this._el.root.classList.remove("hfp-hiding"), this._el.root.classList.add("hfp-visible");
  }
  hide() {
    if (this._el.root.classList.contains("hfp-hiding")) {
      this._hideTimeout || this._scheduleCleanup();
      return;
    }
    this._el.root.classList.contains("hfp-visible") && (this._el.root.classList.add("hfp-hiding"), this._el.root.classList.remove("hfp-visible"), this._scheduleCleanup());
  }
  reset() {
    this._hideTimeout && (clearTimeout(this._hideTimeout), this._hideTimeout = null), this._el.root.classList.remove("hfp-visible", "hfp-hiding"), this._el.fill.style.transform = "scaleX(0)", this._el.transitionValue.textContent = "", this._el.frameValue.textContent = "", this._el.frameRow.style.visibility = "hidden";
  }
  update(e, t) {
    if (t !== "player") {
      this.reset();
      return;
    }
    if (e.ready || !e.loading) {
      this.hide();
      return;
    }
    let i = typeof e.progress == "number" && Number.isFinite(e.progress) ? e.progress : 0, n = typeof e.total == "number" && Number.isFinite(e.total) ? e.total : 0, o = n > 0 ? Math.min(1, Math.max(0, i / n)) : 0, s = Math.min(C.length - 1, Math.floor(o * C.length));
    this._el.title.textContent = C[s] || "Preparing scene transitions", this._el.detail.textContent = e.phase === "cached" ? "Loading cached transition frames before playback." : e.phase === "finalizing" ? "Uploading transition textures for smooth playback." : "Rendering animated scene samples for shader transitions.", this._el.fill.style.transform = `scaleX(${o})`, this._el.transitionValue.textContent = e.currentTransition !== void 0 && e.transitionTotal !== void 0 ? `${e.currentTransition}/${e.transitionTotal}` : n > 0 ? `${i}/${n}` : "";
    let l = e.transitionFrame !== void 0 && e.transitionFrames !== void 0 ? `${e.transitionFrame}/${e.transitionFrames}` : "";
    this._el.frameLabel.textContent = e.phase === "cached" ? "cached transition frames" : e.phase === "finalizing" ? "finalizing transition frames" : "rendering transition frames", this._el.frameValue.textContent = l, this._el.frameRow.style.visibility = l ? "visible" : "hidden", this._el.root.setAttribute("aria-valuenow", String(Math.round(o * 100))), this.show();
  }
  get hideTimeout() {
    return this._hideTimeout;
  }
  destroy() {
    this._hideTimeout && (clearTimeout(this._hideTimeout), this._hideTimeout = null);
  }
  _scheduleCleanup() {
    this._hideTimeout && clearTimeout(this._hideTimeout), this._hideTimeout = setTimeout(() => {
      this._el.root.classList.remove("hfp-hiding"), this._hideTimeout = null;
    }, $e);
  }
};
var We = 0.1;
var qe = 5;
function Y(r) {
  return !Number.isFinite(r) || r <= 0 ? 1 : Math.max(We, Math.min(qe, r));
}
var Q = class extends HTMLElement {
  constructor() {
    super();
    __publicField(this, "shadow");
    __publicField(this, "container");
    __publicField(this, "iframe");
    __publicField(this, "posterEl", null);
    __publicField(this, "controlsApi", null);
    __publicField(this, "resizeObserver");
    __publicField(this, "shaderLoader");
    __publicField(this, "probe");
    __publicField(this, "_ready", false);
    __publicField(this, "_currentTime", 0);
    __publicField(this, "_duration", 0);
    __publicField(this, "_paused", true);
    __publicField(this, "_lastUpdateMs", 0);
    __publicField(this, "_volume", 1);
    __publicField(this, "_compositionWidth", 1920);
    __publicField(this, "_compositionHeight", 1080);
    __publicField(this, "_directTimelineAdapter", null);
    __publicField(this, "_directTimelineClock");
    __publicField(this, "_parentTickRaf", null);
    __publicField(this, "_media");
    this.shadow = this.attachShadow({ mode: "open" }), Ee(this.shadow, me), { container: this.container, iframe: this.iframe } = Se(), this.shadow.appendChild(this.container);
    let e = Le();
    this.shadow.appendChild(e.root), this.shaderLoader = new z(e), this._media = new U({ dispatchEvent: (t) => this.dispatchEvent(t), getMuted: () => this.muted, getVolume: () => this._volume, getPlaybackRate: () => this.playbackRate, getCurrentTime: () => this._currentTime, isPaused: () => this._paused }), this._directTimelineClock = new F({ onTimeUpdate: (t, i) => {
      var _a;
      this._currentTime = t, (_a = this.controlsApi) == null ? void 0 : _a.updateTime(t, i), this.dispatchEvent(new CustomEvent("timeupdate", { detail: { currentTime: t } }));
    }, getLoop: () => this.loop, restart: () => {
      this.seek(0), this.play();
    }, onPaused: () => {
      var _a;
      this._media.audioOwner === "parent" && this._media.pauseAll(), this._paused = true, (_a = this.controlsApi) == null ? void 0 : _a.updatePlaying(false), this.dispatchEvent(new Event("ended"));
    }, onEnded: () => this.loop }), this.probe = new O(this.iframe, { onReady: (t) => this._onProbeReady(t), onError: (t) => this.dispatchEvent(new CustomEvent("error", { detail: { message: t } })) }), this.addEventListener("click", (t) => {
      ye(t) || (this._paused ? this.play() : this.pause());
    }), this.resizeObserver = new ResizeObserver(() => this._rescale()), this._onMessage = this._onMessage.bind(this), this._onIframeLoad = this._onIframeLoad.bind(this);
  }
  static get observedAttributes() {
    return ["src", "srcdoc", "width", "height", "controls", "muted", "volume", "poster", "playback-rate", "audio-src", E, x];
  }
  connectedCallback() {
    this.resizeObserver.observe(this), window.addEventListener("message", this._onMessage), this.iframe.addEventListener("load", this._onIframeLoad), this.hasAttribute("controls") && this._setupControls(), this.hasAttribute("poster") && (this.posterEl = G(this.shadow, this.getAttribute("poster"), this.posterEl)), this.hasAttribute("audio-src") && this._media.setupFromUrl(this.getAttribute("audio-src")), this.hasAttribute("srcdoc") && (this.iframe.srcdoc = j(this, this.getAttribute("srcdoc"))), this.hasAttribute("src") && (this.iframe.src = V(this, this.getAttribute("src")));
  }
  disconnectedCallback() {
    var _a;
    this.resizeObserver.disconnect(), window.removeEventListener("message", this._onMessage), this.iframe.removeEventListener("load", this._onIframeLoad), this.probe.stop(), this._directTimelineClock.stop(), this._stopParentTickClock(), this._directTimelineAdapter = null, this.shaderLoader.destroy(), this._media.destroy(), (_a = this.controlsApi) == null ? void 0 : _a.destroy();
  }
  attributeChangedCallback(e, t, i) {
    var _a, _b, _c, _d, _e2, _f;
    switch (e) {
      case "src":
        i && (this._ready = false, this.iframe.src = V(this, i));
        break;
      case "srcdoc":
        this._ready = false, i !== null ? this.iframe.srcdoc = j(this, i) : this.iframe.removeAttribute("srcdoc");
        break;
      case "width":
        this._compositionWidth = parseInt(i || "1920", 10), this._rescale();
        break;
      case "height":
        this._compositionHeight = parseInt(i || "1080", 10), this._rescale();
        break;
      case "controls":
        i !== null ? this._setupControls() : ((_a = this.controlsApi) == null ? void 0 : _a.destroy(), this.controlsApi = null);
        break;
      case "poster":
        this.posterEl = G(this.shadow, i, this.posterEl);
        break;
      case "playback-rate": {
        let n = Y(parseFloat(i || "1"));
        this._media.updatePlaybackRate(n), this._sendControl("set-playback-rate", { playbackRate: n }), (_c = (_b = this._directTimelineAdapter) == null ? void 0 : _b.timeScale) == null ? void 0 : _c.call(_b, n), (_d = this.controlsApi) == null ? void 0 : _d.updateSpeed(n), this.dispatchEvent(new Event("ratechange"));
        break;
      }
      case "muted":
        this._media.updateMuted(i !== null), this._sendControl("set-muted", { muted: i !== null }), (_e2 = this.controlsApi) == null ? void 0 : _e2.updateMuted(i !== null), this.dispatchEvent(new Event("volumechange"));
        break;
      case "volume": {
        let n = Math.max(0, Math.min(1, parseFloat(i || "1")));
        this._volume = n, this._media.updateVolume(n), this._sendControl("set-volume", { volume: n }), (_f = this.controlsApi) == null ? void 0 : _f.updateVolume(n), this.dispatchEvent(new Event("volumechange"));
        break;
      }
      case "audio-src":
        i && this._media.setupFromUrl(i);
        break;
      case E:
      case x:
        this._reloadShaderOptions();
        break;
    }
  }
  get iframeElement() {
    return this.iframe;
  }
  play() {
    var _a, _b;
    (_a = this.posterEl) == null ? void 0 : _a.remove(), this.posterEl = null, this._duration > 0 && this._currentTime >= this._duration && this.seek(0), this._paused = false;
    let e = this._tryDirectTimelinePlay();
    e || (this._sendControl("play"), this._ready && !this._directTimelineAdapter && this._startParentTickClock()), this._media.audioOwner === "parent" && this._media.playAll(), (_b = this.controlsApi) == null ? void 0 : _b.updatePlaying(true), this.dispatchEvent(new Event("play")), e && this._directTimelineAdapter && this._directTimelineClock.start(this._directTimelineAdapter, () => this._currentTime, () => this._duration, () => this._paused);
  }
  pause() {
    var _a;
    this._tryDirectTimelinePause() || this._sendControl("pause"), this._directTimelineClock.stop(), this._stopParentTickClock(), this._media.audioOwner === "parent" && this._media.pauseAll(), this._paused = true, (_a = this.controlsApi) == null ? void 0 : _a.updatePlaying(false), this.dispatchEvent(new Event("pause"));
  }
  seek(e) {
    var _a, _b;
    !this._trySyncSeek(e) && !this._tryDirectTimelineSeek(e) && this._sendControl("seek", { frame: Math.round(e * 30) }), this._directTimelineClock.stop(), this._stopParentTickClock(), this._currentTime = e, this._media.audioOwner === "parent" && (this._media.pauseAll(), this._media.seekAll(e)), this._paused = true, (_a = this.controlsApi) == null ? void 0 : _a.updatePlaying(false), (_b = this.controlsApi) == null ? void 0 : _b.updateTime(this._currentTime, this._duration);
  }
  get currentTime() {
    return this._currentTime;
  }
  set currentTime(e) {
    this.seek(e);
  }
  get duration() {
    return this._duration;
  }
  get paused() {
    return this._paused;
  }
  get ready() {
    return this._ready;
  }
  get playbackRate() {
    return Y(parseFloat(this.getAttribute("playback-rate") || "1"));
  }
  set playbackRate(e) {
    this.setAttribute("playback-rate", String(Y(e)));
  }
  get shaderCaptureScale() {
    return ke(this);
  }
  set shaderCaptureScale(e) {
    this.setAttribute(E, String(e));
  }
  get shaderLoading() {
    return A(this);
  }
  set shaderLoading(e) {
    e === "composition" ? this.removeAttribute(x) : this.setAttribute(x, e);
  }
  get muted() {
    return this.hasAttribute("muted");
  }
  set muted(e) {
    e ? this.setAttribute("muted", "") : this.removeAttribute("muted");
  }
  get volume() {
    return this._volume;
  }
  set volume(e) {
    this.setAttribute("volume", String(Math.max(0, Math.min(1, e))));
  }
  get loop() {
    return this.hasAttribute("loop");
  }
  set loop(e) {
    e ? this.setAttribute("loop", "") : this.removeAttribute("loop");
  }
  _sendControl(e, t = {}) {
    var _a;
    try {
      (_a = this.iframe.contentWindow) == null ? void 0 : _a.postMessage({ source: "hf-parent", type: "control", action: e, ...t }, "*");
    } catch {
    }
  }
  _reloadShaderOptions() {
    if (A(this) !== "player" && this.shaderLoader.reset(), this.hasAttribute("srcdoc")) {
      this.iframe.srcdoc = j(this, this.getAttribute("srcdoc") || "");
      return;
    }
    this.hasAttribute("src") && (this.iframe.src = V(this, this.getAttribute("src") || ""));
  }
  _trySyncSeek(e) {
    var _a;
    try {
      let i = (_a = this.iframe.contentWindow) == null ? void 0 : _a.__player;
      return typeof (i == null ? void 0 : i.seek) != "function" ? false : (i.seek.call(i, e), true);
    } catch {
      return false;
    }
  }
  _withDirectTimeline(e) {
    let t = this._directTimelineAdapter || this.probe.resolveDirectTimelineAdapter();
    if (!t) return false;
    try {
      return e(t), this._directTimelineAdapter = t, true;
    } catch {
      return false;
    }
  }
  _tryDirectTimelineSeek(e) {
    return this._withDirectTimeline((t) => {
      t.seek(e), t.pause();
    });
  }
  _tryDirectTimelinePlay() {
    return this._withDirectTimeline((e) => {
      e.play();
    });
  }
  _tryDirectTimelinePause() {
    return this._withDirectTimeline((e) => {
      e.pause();
    });
  }
  _startParentTickClock() {
    this._stopParentTickClock();
    let e = () => {
      if (this._paused) {
        this._parentTickRaf = null;
        return;
      }
      this._sendControl("tick"), this._parentTickRaf = requestAnimationFrame(e);
    };
    this._parentTickRaf = requestAnimationFrame(e);
  }
  _stopParentTickClock() {
    this._parentTickRaf !== null && (cancelAnimationFrame(this._parentTickRaf), this._parentTickRaf = null);
  }
  _onMessage(e) {
    Ae(e, this.iframe.contentWindow, { getPlaybackState: () => ({ currentTime: this._currentTime, duration: this._duration, paused: this._paused, lastUpdateMs: this._lastUpdateMs }), setPlaybackState: ({ currentTime: t, duration: i, paused: n, lastUpdateMs: o }) => {
      this._currentTime = t, this._duration = i, this._paused = n, this._lastUpdateMs = o;
    }, getShaderLoadingMode: () => A(this), shaderLoader: this.shaderLoader, setCompositionSize: (t, i) => {
      this._compositionWidth = t, this._compositionHeight = i, this._rescale();
    }, sendControl: (t, i) => this._sendControl(t, i), getIframeDoc: () => this.iframe.contentDocument, updateControlsTime: (t, i) => {
      var _a;
      return (_a = this.controlsApi) == null ? void 0 : _a.updateTime(t, i);
    }, updateControlsPlaying: (t) => {
      var _a;
      return (_a = this.controlsApi) == null ? void 0 : _a.updatePlaying(t);
    }, dispatchEvent: (t) => this.dispatchEvent(t), seek: (t) => this.seek(t), play: () => this.play(), getLoop: () => this.loop, media: this._media });
  }
  _onProbeReady({ duration: e, adapter: t, compositionSize: i }) {
    var _a;
    this._duration = e, this._directTimelineAdapter = t.kind === "direct-timeline" ? t.timeline : null, this._ready = true, (_a = this.controlsApi) == null ? void 0 : _a.updateTime(0, e), this.dispatchEvent(new CustomEvent("ready", { detail: { duration: e } })), i && (this._compositionWidth = i.width, this._compositionHeight = i.height, this._rescale());
    try {
      let n = this.iframe.contentDocument;
      n && this._media.setupFromIframe(n);
    } catch {
    }
    this.hasAttribute("autoplay") && this.play();
  }
  _rescale() {
    Te(this, this.iframe, this._compositionWidth, this._compositionHeight);
  }
  _onIframeLoad() {
    this._directTimelineAdapter = null, this._directTimelineClock.stop(), this._stopParentTickClock(), this.shaderLoader.reset(), this._media.resetForIframeLoad(), this.probe.start();
  }
  _setupControls() {
    this.controlsApi || (this.controlsApi = _e(this.shadow, this.muted, this._volume, this.getAttribute("speed-presets"), { onPlay: () => this.play(), onPause: () => this.pause(), onSeek: (e) => this.seek(e * this._duration), onSpeedChange: (e) => {
      this.playbackRate = e;
    }, onMuteToggle: () => {
      this.muted = !this.muted;
    }, onVolumeChange: (e) => {
      this.volume = e;
    } }));
  }
  get _audioOwner() {
    return this._media.audioOwner;
  }
  get _parentMedia() {
    return this._media.entries;
  }
  _mirrorParentMediaTime(e, t) {
    this._media.mirrorTime(e, t);
  }
  _promoteToParentProxy() {
    let e = null;
    try {
      e = this.iframe.contentDocument;
    } catch {
    }
    this._media.promoteToParentProxy(e, (t, i) => this._mirrorParentMediaTime(t, i)), this._sendControl("set-media-output-muted", { muted: true });
  }
  _observeDynamicMedia(e) {
    this._media.setupFromIframe(e);
  }
};
customElements.get("hyperframes-player") || customElements.define("hyperframes-player", Q);
export {
  Q as HyperframesPlayer,
  ve as SPEED_PRESETS,
  H as formatSpeed,
  B as formatTime
};
//# sourceMappingURL=@hyperframes_player.js.map
