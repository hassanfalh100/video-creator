/**
 * Recast-free GSAP helpers: serialization, keyframe<->animation conversion,
 * validation, and shared types.
 *
 * This module MUST NOT import recast / @babel/parser. It is part of the
 * isomorphic core layer that the barrel and browser code depend on. AST
 * parsing of GSAP source lives in the Node-only `./gsapParser` module.
 */
import type { Keyframe, ValidationResult } from "../core.types";
export type GsapMethod = "set" | "to" | "from" | "fromTo";
export interface GsapAnimation {
    id: string;
    targetSelector: string;
    method: GsapMethod;
    position: number | string;
    properties: Record<string, number | string>;
    fromProperties?: Record<string, number | string>;
    duration?: number;
    ease?: string;
    /** Non-editable GSAP config (stagger, yoyo, repeat, etc.) preserved for round-trips. */
    extras?: Record<string, unknown>;
}
export interface ParsedGsap {
    animations: GsapAnimation[];
    timelineVar: string;
    preamble: string;
    postamble: string;
    multipleTimelines?: boolean;
    unsupportedTimelinePattern?: boolean;
}
export { SUPPORTED_PROPS, SUPPORTED_EASES } from "./gsapConstants";
export declare function serializeGsapAnimations(animations: GsapAnimation[], timelineVar?: string, options?: {
    includeMediaSync?: boolean;
    preamble?: string;
    postamble?: string;
}): string;
/**
 * Filter animations to those targeting `#<elementId>` (id-only match). For the
 * studio panel's id-OR-selector matching, see `getAnimationsForElement` in
 * `useGsapTweenCache.ts` — distinct on purpose, hence the distinct name.
 */
export declare function getAnimationsForElementId(animations: GsapAnimation[], elementId: string): GsapAnimation[];
export declare function validateCompositionGsap(script: string): ValidationResult;
export declare function keyframesToGsapAnimations(elementId: string, keyframes: Keyframe[], elementStartTime: number, base?: {
    x?: number;
    y?: number;
    scale?: number;
}): GsapAnimation[];
export declare function gsapAnimationsToKeyframes(animations: GsapAnimation[], elementStartTime: number, options?: {
    baseX?: number;
    baseY?: number;
    baseScale?: number;
    clampTimeToZero?: boolean;
    skipBaseSet?: boolean;
}): Keyframe[];
//# sourceMappingURL=gsapSerialize.d.ts.map