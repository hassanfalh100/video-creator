export type OpenTag = {
    raw: string;
    name: string;
    attrs: string;
    index: number;
};
export type ExtractedBlock = {
    attrs: string;
    content: string;
    raw: string;
    index: number;
};
export declare const STYLE_BLOCK_PATTERN: RegExp;
export declare const SCRIPT_BLOCK_PATTERN: RegExp;
export declare const TIMELINE_REGISTRY_INIT_PATTERN: RegExp;
export declare const TIMELINE_REGISTRY_ASSIGN_PATTERN: RegExp;
export declare const WINDOW_TIMELINE_ASSIGN_PATTERN: RegExp;
export declare const INVALID_SCRIPT_CLOSE_PATTERN: RegExp;
export declare function extractOpenTags(source: string): OpenTag[];
export declare function extractBlocks(source: string, pattern: RegExp): ExtractedBlock[];
/**
 * Find the `<html>` open tag in the source. Distinct from `findRootTag`,
 * which returns the first element inside `<body>` — the latter is "the
 * composition's visible root", whereas `<html>` is where document-level
 * metadata like `data-composition-variables` lives.
 */
export declare function findHtmlTag(source: string): OpenTag | null;
export declare function findRootTag(source: string): OpenTag | null;
export declare function readAttr(tagSource: string, attr: string): string | null;
/**
 * Read an attribute that may legitimately contain the opposite quote
 * character. `readAttr` truncates `data-variable-values='{"title":"Hello"}'`
 * at the first internal `"` because its `[^"']+` class excludes both quote
 * types. This variant alternates: a double-quoted value never contains an
 * unescaped `"`, and a single-quoted value never contains an unescaped `'`,
 * so each branch can use a quote-specific class.
 *
 * Use for attributes whose values are JSON or otherwise carry the opposite
 * quote character. Existing single-token attributes (`id`, `class`, etc.)
 * stick with `readAttr` for consistency with the rest of the lint code.
 */
export declare function readJsonAttr(tagSource: string, attr: string): string | null;
export declare function collectCompositionIds(tags: OpenTag[]): Set<string>;
export declare function extractCompositionIdsFromCss(css: string): string[];
export declare function getInlineScriptSyntaxError(source: string): string | null;
export declare function isMediaTag(tagName: string): boolean;
export declare function truncateSnippet(value: string, maxLength?: number): string | undefined;
//# sourceMappingURL=utils.d.ts.map